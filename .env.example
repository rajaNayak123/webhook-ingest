HTTP_ADDR=
DATABASE_URL=
REDIS_ADDR=
DB_MAX_CONNS=

# Host ports published by docker compose. Override these if you already run
# Postgres or Redis locally — then point DATABASE_URL / REDIS_ADDR at them too.
APP_PORT=
POSTGRES_PORT=
REDIS_PORT=
